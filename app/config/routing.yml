app:
    resource: "@AppBundle/Controller/"
    type:     annotation

api:
    resource: "@AppBundle/Controller/Api"
    type:     annotation
    prefix:   "/api/"

modules:
    resource: ..
    type:     modules

legacy:
    resource: .
    type: phpfiles
