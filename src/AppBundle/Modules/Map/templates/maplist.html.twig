{% extends 'base.html.twig' %}

{% block body %}
    <h1>Alle aktuellen Karten</h1>

    {{ form_start(filter_form) }}
    {{ form_widget(filter_form) }}
    <button>Such!</button>
    {{ form_end(filter_form) }}
    <table>
        <tbody>
            <tr>
                <th>Vorschau</th>
                <th>Name</th>
                <th>Spieler</th>
                <th>Züge</th>
                <th>Ersteller</th>
            </tr>
            {% for map in maps %}
                <tr>
                    <td>
                        <a href="/map/{{ map.id }}">
                            <img src="/images/maps/thumbnails/{{ map.id }}.png" width="60" border="0">
                        </a>
                    </td>
                    <td>
                        <b id="map{{ map.id }}">{{ map.id }}:
                            <a href="/map/{{ map.id }}">
                                {{ map.name }}
                            </a></b>
                    </td>
                    <td>
                        {{ map.players }}
                    </td>
                    <td>
                        {{ map.record }}
                    </td>
                    <td>
                        {{ map.author }}
                    </td>

                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
